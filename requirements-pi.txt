# ============================================================
# requirements-pi.txt
# Raspberry Pi 4B Runtime Dependencies
# OS: Raspberry Pi OS Bookworm 64-bit (Python 3.11)
# Install with: pip install -r requirements-pi.txt
#
# PREREQUISITES (install via apt BEFORE pip):
#   sudo apt update && sudo apt upgrade -y
#   sudo apt install python3-pip python3-venv libopenblas-dev \
#     python3-smbus i2c-tools libatlas-base-dev \
#     libavcodec-dev libavformat-dev libswscale-dev -y
# ============================================================

# --- AI Inference (no PyTorch needed on Pi) ---
onnxruntime==1.19.2          # CPU-optimized ONNX inference engine for ARM64

# --- Computer Vision ---
opencv-python-headless==4.10.0.84  # Headless OpenCV (no GUI deps, lighter on Pi)
numpy==1.26.4                      # Numerical arrays

# --- Thermal Camera (AMG8833 via I2C) ---
adafruit-circuitpython-amg88xx==1.3.12  # AMG8833 driver
adafruit-blinka==8.45.0                 # CircuitPython compatibility layer
RPi.GPIO==0.7.1                         # GPIO access

# --- Ambient Sensor (DHT22) ---
adafruit-circuitpython-dht==3.7.9          # DHT22 temp+humidity driver (Bookworm compatible)

# --- GSM900A SMS Module ---
pyserial==3.5                           # Serial communication via UART

# --- Object Tracking ---
filterpy==1.4.5              # Kalman filters (used by SORT tracker)
scipy==1.13.1                # scipy.optimize (Hungarian algorithm for SORT)

# --- Web Dashboard ---
Flask==3.0.3                 # Lightweight web framework
Jinja2==3.1.4                # HTML templating (bundled with Flask)
Werkzeug==3.0.4              # WSGI utils (bundled with Flask)

# --- Database ---
# SQLite3 is built into Python — no pip install needed

# --- Data Processing ---
PyYAML==6.0.2                # Config file parsing
Pillow==10.4.0               # Image save for snapshots

# --- Utilities ---
tqdm==4.66.5                 # Progress bars (useful for DB migrations)
